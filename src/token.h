#ifndef TOKEN_H
#define TOKEN_H

enum token_t {
  TOKEN_EOF = 0,
  TOKEN_LBRAC,
  TOKEN_RBRAC,
  TOKEN_LPAREN,
  TOKEN_RPAREN,

  TOKEN_TITLE,
  TOKEN_HEADING,
  TOKEN_SUBHEADING,
  TOKEN_BOLD,
  TOKEN_ITALIC,
  TOKEN_BULLET,

  TOKEN_STRING_LITERAL,
  TOKEN_TEXT,
  TOKEN_ERROR,

};

#endif
